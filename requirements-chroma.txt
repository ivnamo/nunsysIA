-r requirements.txt

# Optional ChromaDB adapter.
# Needed for CHROMA_MODE=persistent or for connecting to a Chroma HTTP server.
# If chromadb is not installed or not reachable/openable, the app falls back to memory.
chromadb>=1.5,<1.6
