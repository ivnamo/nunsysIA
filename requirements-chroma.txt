-r requirements.txt

# Optional local ChromaDB adapter.
# On Windows this can require native build tooling because of chroma-hnswlib.
# For the POC demo, the app falls back to the in-memory vector store when
# chromadb is not installed or not reachable.
chromadb>=0.5,<0.6
langchain-chroma>=0.1,<0.2
