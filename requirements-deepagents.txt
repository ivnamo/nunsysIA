# Opcional: solo para probar el sidecar `app.agents.deepagents_adapter`.
# No forma parte del runtime principal, porque deepagents actual requiere
# LangChain/LangGraph 1.x y la POC estable esta fijada en 0.x.
deepagents>=0.6,<0.7
langchain>=1.3,<2.0
langgraph>=1.2,<1.3
langchain-google-genai>=4.2,<5.0
